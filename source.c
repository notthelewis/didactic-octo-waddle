#include <stdio.h>

/* Multi-line on single-line */
/* 
    Multi
    Line
 */
// Single line which starts at the very start of a line.
//Single line which starts at the very start of a line, but also has no whitespace after the 
int main(int argc, char const *argv[])
{
    // Single line comment which begins with whitespace
    return 0;
}